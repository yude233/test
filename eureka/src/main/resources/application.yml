server:
  port: 8761  #Eureka服务器要监听的端口
eureka:
  client:
    register-with-eureka: false  #不要使用Eureka服务进行注册
    fetch-registry: false #不要在本地缓存注册表信息
  server:
    wait-time-in-ms-when-sync-empty: 1  #在服务器接收请求之前等待的初始时间
    enable-self-preservation: false